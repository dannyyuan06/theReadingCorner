/** @type {import('next').NextConfig} */
const nextConfig = { images: { domains: ['books.google.com', "lh3.googleusercontent.com", "storage.googleapis.com"], formats: ['image/avif', 'image/webp'], }, }

module.exports = nextConfig
